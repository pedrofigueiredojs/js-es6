// Retorne o url da página atual utilizando o objeto window
const urlPagina = window.location.href;
console.log(urlPagina);

// Seleciona o primeiro elemento da página que
// possua a classe ativo
const classeAtiva = document.querySelectorAll(".ativo");
console.log(classeAtiva);

// Retorne a linguagem do navegador
const idiomaNavegador = window.navigator.language;
console.log(idiomaNavegador);

// Retorne a largura da janela
const larguraJanelaNavegador = window.innerWidth;
console.log(larguraJanelaNavegador);
